import {
    jest,
    describe,
    test,
    expect,
    afterEach,
    beforeEach
} from '@jest/globals';

import TemplatePreviewer from '../../src/services/TemplatePreviewer';

describe('services/TemplatePreviewer', () => {
    test('Initialization', () => {
        // expect(() => TemplatePreviewer()).not.toThrow();
    });
});
