import generate from './generate';
import preview from './preview';
import template from './template';
import setProp from './setProp';

export default { generate, preview, template, 'set-prop': setProp };
